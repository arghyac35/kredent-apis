import attachCurrentUser from './attachCurrentUser';
import isAuth from './isAuth';
import attachOwnsToken from './attachOwnsToken';

export default {
  attachCurrentUser,
  isAuth,
  attachOwnsToken,
};
